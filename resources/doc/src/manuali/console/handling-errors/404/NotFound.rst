.. _errori_404_NotFound:

NotFound
--------

Il contenuto della richiesta non indirizza una API esistente su GovWay.

Questo tipo di errore avviene nei Profili di Interoperabilità per i quali l'API non viene indirizzata nella URL ma all'interno del contenuto della richiesta (es. :ref:`profiloSPCoop`).
