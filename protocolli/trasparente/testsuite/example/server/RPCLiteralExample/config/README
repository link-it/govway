Si genera solo il literal, perchè encoded non è più supportato da cxf.

Inoltre si sono è modificato a mano il metodo rpclElement delle classi ServiceRPCLiteral.java per aggiungere la gestione tramite  JAXBElement<...>
