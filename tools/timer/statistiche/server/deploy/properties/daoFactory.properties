# ================================================
# GovWay - Statistiche Server DAO Factory Configuration
# ================================================

# Tipo Database
db.tipoDatabase=postgresql

# Debug SQL
db.showSql=false

# Tipo di accesso: datasource (JNDI)
db.tipo=datasource

# Nome JNDI del DataSource (configurato in WildFly)
db.datasource.jndiName=org.govway.datasource
