Il processo di build richiede memoria (soprattutto per quanto riguarda il PermGen space).
Se si ottiene un out of memory regolare la memoria fornita ad ant impostando la seguente variabile di ambiente:

export ANT_OPTS="-Xmx2048m -XX:MaxMetaspaceSize=2048m"
